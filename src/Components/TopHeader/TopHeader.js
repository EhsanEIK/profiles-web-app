import React from 'react';
import banner from '../../assets/images/cover.png';

const TopHeader = () => {
    return (
        <section>
            <img src={banner} alt="" />
        </section>
    );
};

export default TopHeader;